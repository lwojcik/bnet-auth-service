export enum RedisEnvVariable {
  enable = 'ENABLE',
  host = 'HOST',
  port = 'PORT',
  password = 'PASSWORD',
  ttlSecs = 'TTL_SECS',
  db = 'DB',
  keyPrefix = 'KEY_PREFIX',
  keyName = 'KEY_NAME',
}
